# Backend API base URL — the address that **client browsers** can reach.
#
# 留空时，前端会自动用 window.location.hostname:8000 推导后端地址，
# 这样 localhost 和局域网 IP 访问都会自动指向"当前打开的主机"的 8000。
# 大多数情况下保持留空即可。
#
# 只在以下场景需要显式指定：
#   - 前端和后端不在同一台机器：VITE_API_URL=http://192.168.1.10:8000
#   - 公网部署带反代 + HTTPS：    VITE_API_URL=https://docchat.example.com
#
# The WebSocket endpoint (/chat/stream) is derived from this URL by
# replacing http→ws / https→wss in src/services/websocket.ts, so do not
# add a separate VITE_WS_URL.
# VITE_API_URL=
